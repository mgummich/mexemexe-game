{
  "name": "mexemexe-game",
  "private": true,
  "version": "1.11.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview --port 4173 --strictPort",
    "test": "vitest run --coverage",
    "test:watch": "vitest",
    "test:server": "vitest run tests/server",
    "test:property": "MEXE_FUZZ=extended vitest run tests/property --testTimeout=60000",
    "test:replay": "vitest run tests/replay.test.ts",
    "test:mutation": "stryker run",
    "server": "tsx server/index.ts",
    "replay": "tsx scripts/replay.ts",
    "simulate": "tsx scripts/simulate.ts",
    "health": "node scripts/health.mjs",
    "gen:cosmetics": "node scripts/gen-cosmetics.mjs",
    "gen:icons": "node scripts/gen-icons.mjs",
    "lint": "eslint src tests e2e e2e-cross e2e-multiplayer e2e-pwa server && tsc --noEmit",
    "screenshot": "npm run build && playwright test e2e/screenshot.spec.ts --grep @perf --workers=1 --retries=0 && MEXE_KEEP_VERIFY_LOG=1 playwright test e2e/screenshot.spec.ts --grep-invert @perf",
    "verify": "npm run test && npm run lint && npm run screenshot && node scripts/check-verify.mjs",
    "verify:multiplayer": "npm run build && playwright test -c playwright.multiplayer.config.ts && node scripts/check-verify-multiplayer.mjs",
    "verify:multiplayer:chromium": "npm run build && playwright test -c playwright.multiplayer.config.ts --project=chromium --grep-invert @chaos && node scripts/check-verify-multiplayer.mjs --engines=chromium",
    "verify:cross": "npm run build && playwright test -c playwright.cross.config.ts",
    "verify:pwa": "npm run build && playwright test -c playwright.pwa.config.ts",
    "verify:visual": "npm run build && playwright test e2e/visual.spec.ts --workers=1 --retries=0",
    "verify:preview": "npm run build && node scripts/check-preview.mjs",
    "release": "node scripts/release.mjs"
  },
  "dependencies": {
    "phaser": "^4.2.1",
    "tsx": "^4.23.13",
    "ws": "^8.21.3"
  },
  "devDependencies": {
    "@playwright/test": "^1.49.0",
    "@stryker-mutator/core": "^10.0.0",
    "@types/node": "^26.6.1",
    "@types/ws": "^8.18.1",
    "@vitest/coverage-v8": "^5.0.1",
    "eslint": "^10.10.0",
    "typescript": "^7.0.2",
    "typescript-eslint": "^8.18.0",
    "vite": "^8.3.0",
    "vitest": "^5.0.0"
  },
  "allowScripts": {
    "esbuild@0.25.12": true,
    "esbuild@0.21.5": true
  }
}
